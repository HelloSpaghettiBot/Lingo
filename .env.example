# Required: token from the Discord Developer Portal.
DISCORD_TOKEN=replace_with_your_bot_token

# Optional: use a server ID while developing so slash-command changes appear immediately.
# Remove this line for global commands in every server where the bot is installed.
DISCORD_GUILD_ID=

# Optional settings.
BOT_CONFIG_PATH=config.json
LOG_LEVEL=INFO
